## Add a TODO comment indicating the reason for each rejected dependency upgrade added to this list, and what should be done to resolve it (i.e. handle it through a story, etc).
reject: [
  # TODO: v6+ (ref: https://github.com/sindresorhus/get-port/releases/tag/v6.0.0) is an ESM library and thus not compatible with CommonJS. Future story needed to resolve.
  "get-port",
  # TODO: v15+ (ref: https://github.com/tj/commander.js/releases/tag/v15.0.0) is ESM-only and breaks the CommonJS `require('commander')` in src/index.js + test/unit/index.test.js. Future story needed to migrate the CLI entrypoint to ESM.
  "commander",
  # TODO: Babel v8 (ref: https://babeljs.io/blog/2025/babel-8) is a major release; defer until the jest/babel transform pipeline is validated against it. Future story needed to upgrade.
  "@babel/core",
  "@babel/preset-env"
]

